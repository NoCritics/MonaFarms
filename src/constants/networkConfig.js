/**
 * Network configuration for MonaFarms application
 */

// Chain ID for Monad testnet
export const CHAIN_ID = 10143;

// Network configuration
export const NETWORK_CONFIG = {
  chainId: CHAIN_ID,
  name: 'Monad Testnet',
  currency: {
    name: 'Monad',
    symbol: 'MON',
    decimals: 18
  }
};

export default NETWORK_CONFIG; 